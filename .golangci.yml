version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - gocritic
    - ineffassign
    - misspell
    - unconvert
  exclusions:
    generated: strict

formatters:
  enable:
    - gofmt
